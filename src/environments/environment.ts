export const environment = {
  production: false,
  apiBaseUrl: 'http://127.0.0.1:5000',
};

// Si no se quiere usar docker local, se puede usar la siguiente URL:
// apiBaseUrl: 'http://localhost:5000'